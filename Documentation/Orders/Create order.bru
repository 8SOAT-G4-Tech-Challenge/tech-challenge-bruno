meta {
  name: Create order
  type: http
  seq: 3
}

post {
  url: {{BASE_URL}}/orders
  body: json
  auth: none
}

body:json {
  {
    // You can assign a customer to each order
    // "customerId": "{{CUSTOMER_ID}}"
  }
}
